{
  "name": "neighbors-in-the-loop",
  "version": "1.0.0",
  "private": true,
  "description": "A local business discovery app backed by SQLite and a Node API.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --test tests/api.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "sqlite3": "^6.0.1"
  }
}
